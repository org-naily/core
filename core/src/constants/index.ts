export const enum NailyWatermark {
  VALUE = "__naily:configure:value__",
  INJECTABLE = "__naily:ioc:injectable__",
  CONFIGURATION = "__naily:ioc:configuration__",
  INJECT = "__naily:ioc:inject__",
}

export const enum ScopeEnum {
  SINGLETON = "__singleton__",
  PROTOTYPE = "__PROTOTYPE__",
}
