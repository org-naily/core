export const enum NailyIocWatermark {
  INJECTABLE = "naily:injectable",
}
export const enum ScopeOption {
  SINGLETON = "__singleton__",
  TRANSIENT = "__transient__",
}

export type IScopeOption = "__singleton__" | "__transient__";

export const enum NailyLogWatermark {
  USE = "naily:logger:use",
}
