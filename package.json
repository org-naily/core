{
  "name": "v10",
  "scripts": {
    "dev": "pnpm -F test dev",
    "build:core": "pnpm -F @naily/core build",
    "build:web": "pnpm -F @naily/web build",
    "build:express": "pnpm -F @naily/web-express build",
    "build:docs": "pnpm -F @naily/docs docs:build",
    "dev:docs": "pnpm -F @naily/docs docs:dev",
    "preview:docs": "pnpm -F @naily/docs docs:preview"
  },
  "dependencies": {
    "@types/node": "^20.8.6",
    "reflect-metadata": "^0.1.13",
    "ts-morph": "^20.0.0",
    "typescript": "^5.2.2"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public",
    "tag": "latest"
  }
}
