{
  "name": "test",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc && node dist/src/main",
    "generate": "tsc && node dist/src/main.spec"
  },
  "dependencies": {
    "@naily/core": "workspace:*",
    "@naily/web": "workspace:*",
    "@naily/web-express": "workspace:*"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
